# Shared prompts

Drop reusable `.md` prompt files here. Pi loads every `.md` file in this
directory as a prompt available across the org.

Keep each prompt focused and self-contained. Name files by intent, e.g.
`review-checklist.md`, `incident-postmortem.md`.
