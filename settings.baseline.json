{
  "$comment": "Copy this file to `.pi/settings.json` in a project repo and commit it. Teammates run `pi`, approve the trust prompt once, and Pi auto-installs the standard package on startup. Pin to a git tag for reproducibility — there is no lockfile, so the pinned ref IS your determinism.",
  "packages": [
    "git:github.com/yourorg/pi-standard@v0.1.0"
  ]
}
