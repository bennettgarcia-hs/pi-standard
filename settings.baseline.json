{
  "$comment": "Copy this file to `.pi/settings.json` in a project repo and commit it. Teammates run `pi`, approve the trust prompt once, and Pi auto-installs the standard package on startup. Pin to an exact published version for reproducibility (published versions are immutable). Registry auth via GitHub Packages is required once per machine — see the README.",
  "packages": [
    "npm:@bennettgarcia-hs/pi-standard@0.4.0"
  ]
}
